# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed

# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created, ready for development
#   - in-progress: Developer actively working on implementation
#   - review: Implementation complete, ready for review
#   - done: Story completed
#   - moved-to-epic-<N>: Terminal. The story left this epic for another; the epic it left is
#     SMALLER, not blocked. The comment above the key carries the ruling that moved it.
#   - deferred-to-epic-<N>: Terminal HERE, open THERE. The work is still owed, by a later epic.
#     Neither value is 'done' and neither is 'backlog' -- both would erase the routing.
#   - deprecated: Terminal. The story will not be built as written; its intent is carried by work
#     OUTSIDE the epic structure (a spec's stories.yaml). The comment above the key names where the
#     intent went and what it inherited, so nothing the story owed evaporates with it.

# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed

# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed

# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when its first story starts (via build's sprint sync)
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates the next story after the previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; the status view surfaces open ones
generated: 08-23-2026 09:38
last_updated: 09-10-2026
project: folio
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts
development_status:
  # ---------------------------------------------------------------------------
  # DO NOT run `sprint_plan.py generate` against this file without first
  # re-adding the four inserted stories below. Verified 2026-08-29: the script
  # regenerates development_status FROM epics.md and drops anything absent there.
  # 2-3a, 2-5a, 2-6a and 3-1a are all `done`, all have story files on disk, and
  # are all DELIBERATELY absent from epics.md — each story file states it in
  # bold ("This story does not come from epics.md ... epics.md is not amended by
  # this story and must not be"), per D-000.25, D-2.6.9 and D-3.1a.1. So the
  # omission is a RULING, not drift, and must not be "fixed" by amending
  # epics.md. `--set` cannot restore them either: it only retargets keys the
  # generated plan already contains. A bare `generate` run therefore destroys
  # the record of four completed stories. Reconcile by hand, as of this update.
  # ---------------------------------------------------------------------------
  epic-1: done
  1-1-a-minimal-pdf-reproducible-on-one-machine: done
  1-2-cross-target-byte-identity-proven-in-ci: done
  1-3-guardrails-that-fail-the-build: done
  1-4-load-validate-and-round-trip-a-folio-template: done
  1-5-embed-and-subset-a-font-byte-stably: done
  1-6-bind-scalar-json-values-into-text: done
  1-7-render-to-an-io-writer-with-runtime-parameters: done
  1-8-embed-and-render-an-image-from-the-template: done
  epic-1-retrospective: optional

  # epic-2 closed 2026-08-25 over one REQUIRED red (TestCorpusMeetsP6ExerciseFloors,
  # P6g 7<20) which D-000.17/D-2.1.14 mandate stay unmet. See epic-2-boundary-gate.md.
  epic-2: done
  2-1-thai-break-opportunity-spike: done
  2-2-the-shipped-font-set-and-its-fallback-chain: done
  2-3-shape-latin-thai-and-cjk-text: done
  # 2-3a inserted between 2.3 and 2.4 per D-000.25 (vendor-boundary audit +
  # type-aware AD-23 guard). Must land before 2.4 adds vendor call sites.
  2-3a-audit-the-vendor-boundary: done
  2-4-break-and-measure-lines-in-all-three-scripts: done
  2-5-compose-a-page-from-three-bands: done
  # 2-5a inserted before 2.6 to discharge DW-15 (first baseline placed from font
  # size vs inter-baseline spacing from hhea ascent). Must land BEFORE the Thai
  # reading sign-off is recorded: it re-records shaped-text/expected.pdf, whose
  # digest that sign-off binds to (D-000.26 refined).
  2-5a-align-first-baseline-with-the-leading-model: done
  2-6-render-multi-page-documents-with-headers-and-footers-on-ever: done
  # 2-6a inserted between 2.6 and 2.7 per D-2.6.9 (behavioural sweep of the shared
  # PDF-reading test helpers for single-page-shape assumptions). Must land BEFORE
  # 2.7, which adds a run on every page and lands directly on those helpers.
  2-6a-sweep-single-page-assumptions-from-the-shared-pdf-readers: done
  2-7-render-page-x-of-y: done
  2-8-clip-overflowing-content-and-say-so: done
  epic-2-retrospective: optional

  # Boundary gate passed 2026-08-26: matrix green on 4 targets x 10 documents,
  # 4 blockers closed, 11 items carried forward. See epic-3-boundary-gate.md.
  epic-3: done
  3-1-bind-a-repeating-region-to-a-collection-with-an-explicit-row: done
  # 3-1a inserted between 3.1 and 3.2 per D-3.1a.1, on D-000.25's precedent (which
  # inserted 2.3a for this same guard). AD-23's float ban is keyed on float32/float64
  # and cannot see math/big.Float. Must land BEFORE 3.2 creates internal/expr.
  3-1a-guard-ad-23-against-named-binary-float-types: done
  3-2-evaluate-the-expression-language: done
  3-3-aggregate-over-a-collection: done
  3-4-format-dates-and-numbers-by-declared-locale: done
  3-5-hide-a-component-conditionally: done
  3-6-fail-with-located-actionable-diagnostics: done
  3-7-validate-a-template-and-render-it-from-the-command-line: done
  epic-3-retrospective: optional

  epic-4: done
  4-1-lay-out-a-table-s-columns-header-and-cell-chrome: done
  4-2-generate-rows-from-a-bound-collection: done
  4-3-break-a-table-across-pages-without-splitting-a-row: done
  4-4-repeat-the-table-header-on-every-continuation-page: done
  4-5-render-footer-aggregates-without-orphaning-them: done
  4-6-handle-a-row-taller-than-the-page: done
  4-7-the-golden-report-renders-and-hashes-match: done
  4-8-alternating-row-styling: done
  epic-4-retrospective: optional

  # Boundary gate passed 2026-08-28: 14/14 real Playwright scenarios, offline
  # release verification, and the 4-target x 15-document matrix are green.
  # See epic-5-boundary-gate.md.
  # Reopened 2026-08-29: Story 5.13 completes FR4/FR5 for the Image component, which
  # Stories 5.7/5.8 left without any way to choose the picture. The gate above stands as
  # baseline-anchored evidence for 5.1-5.12; a second Epic 5 boundary is owed once 5.13 lands.
  # Second boundary gate passed 2026-08-29 at 6c79d29: 23/23 Playwright scenarios,
  # the 4-target x 16-document matrix (incl. Story 5.13's component-asset-import),
  # offline release verification, and the post-Epic-6-gate delta. See
  # epic-5-boundary-gate-2.md. DW-23 filed for a pre-existing lint gofmt break.
  epic-5: done
  5-1-the-design-system-and-application-shell: done
  5-2-the-engine-worker-and-command-channel: done
  5-3-work-offline: done
  5-4-an-honest-first-run-load-screen: done
  5-5-open-and-save-folio-files-locally: done
  5-6-the-canvas-page-bands-grid-and-page-setup: done
  5-7-place-and-manipulate-the-five-components: done
  5-8-edit-component-properties: done
  5-9-a-canvas-the-browser-never-measures: done
  5-10-preview-the-exact-production-document: done
  5-11-never-show-a-stale-preview: done
  5-12-diagnostics-that-locate-and-an-interface-that-can-be-driven: done
  5-13-import-an-image-and-set-it-on-an-image-component: done
  epic-5-retrospective: optional

  epic-6: done
  6-1-load-sample-data-and-browse-its-paths: done
  6-2-bind-a-component-by-picking-a-path: done
  6-3-supply-parameters-for-preview: done
  6-4-configure-table-columns-as-a-matrix: done
  6-5-bind-columns-in-row-scope-and-configure-footer-aggregates: done
  6-6-present-a-failed-render-honestly: done
  6-7-the-round-trip-closes: done
  epic-6-retrospective: optional

  # Epic 7 added 2026-08-29 (post-MVP). Long-form body text + a multi-page authoring
  # canvas, for legal-document templates. Scoped in conversation against the alternative
  # of per-page layouts, which was REJECTED: paginate.go's window model (D-2.6.1, rule 1
  # — "more windows, never rearranged furniture") and the page-count-N/statement-N golden
  # corpus both rest on one unbounded content column whose page count comes from DATA.
  # Per-page layout implies content that never reflows, i.e. silently clipped clause text.
  # Epic 7 changes the AUTHORING SURFACE and TYPOGRAPHY only; pagination is a constraint
  # on it, not a target of it. Every field it adds is optional and absent-by-default, so
  # the existing corpus must hash identically.
  # Boundary gate run 2026-08-31, PASSED -- see epic-7-8-decision-log.md, EPIC 7 BOUNDARY
  # GATE. Story 7.6's AC2 was FALSE at HEAD from ed485eb until 7.9 landed, which is why this
  # epic stayed open for two extra stories. 7.10 remains backlog: it does NOT gate the epic
  # (D-7.7.13) but MUST precede the folio-go/v0.1.0 tag.
  epic-7: done
  7-1-break-a-line-where-the-author-typed-a-break: done
  7-2-set-the-space-between-a-paragraph-s-lines: done
  7-3-justify-a-paragraph-s-edges: done
  # 7.4 owns DW-25 and DW-29 as acceptance criteria, not deferrals. See
  # epic-7-8-decision-log.md D-7.4.1..D-7.4.5.
  7-4-author-body-text-in-the-designer: done
  7-5-the-content-column-runs-past-the-first-page: done
  7-6-the-canvas-draws-every-page-the-document-will-produce: done
  7-7-keep-a-signature-block-together-across-a-page-break: done
  # 7.8 added 2026-08-30 by Story 7.4's plan gate, which routed DW-29 out.
  7-8-refuse-a-justified-table-at-load-in-the-author-s-own-terms: done
  # 7.9 and 7.10 added 2026-08-31 by the engineering lead's rulings at Story 7.7's close
  # (D-7.7.6..D-7.7.11). Both REPAIR Epic 7 rather than extending it, which is the line the
  # lead drew for adding stories to a scope-closed epic. 7.9 GATES `epic-7: done` -- Story
  # 7.6's AC2 is false at HEAD for a grouped document. 7.10 does NOT gate the epic, but it
  # must land before the folio-go/v0.1.0 tag: it narrows what renders, which is free only
  # while nothing is released (AD-22).
  7-9-the-canvas-tells-the-truth-about-keep-together-groups: done
  7-10-an-over-tall-element-is-refused-whether-or-not-it-is-grouped: done
  epic-7-retrospective: optional

  # Epic 8 added 2026-08-30 (post-MVP). Fonts an author can choose and a file can carry,
  # from SPEC-fonts (_bmad-output/specs/spec-fonts). Scoped in conversation against the
  # alternative of a zip-of-folders .folio, which was REJECTED: the format exists so a
  # person or an agent can edit a template without the designer (FR12) and so a
  # hand-written template renders (S9), and a container puts entry order, timestamps and
  # compression inside the byte-identity regime. Font bytes therefore ride the existing
  # content-addressed assets map. Bold/italic are NOT in scope — they need a face
  # inventory decision SPEC-fonts leaves open. Every field is optional and
  # absent-by-default, so the existing corpus must hash identically.
  # Boundary gate run 2026-09-02 at f2db371, PASSED -- see epic-8-boundary-gate.md. All three owed
  # suites RAN: the four matrix legs, TestCrossTargetByteIdentity, and PLAYWRIGHT, which executed for
  # the FIRST TIME IN THIS REPOSITORY'S HISTORY (24 tests, 12 spec files, real browser). Two caveats
  # stated rather than smoothed: it ran against Chrome 149 because the pinned revision 1208 is
  # UNDOWNLOADABLE (the host returns HTTP 400), so the pinned-revision leg is undischarged; and the
  # run exposed evidence/story-6.7-roundtrip-manifest.json as stale -- commit 6c79d29 calls itself
  # "the re-run 6.7 manifest" but Playwright had never run, so it cannot have come from a real run.
  # Zero new failures; three standing reds by identity. 8.4d and the fetch tier moved to Epic 15.
  epic-8: done
  # 8.0 added 2026-08-31: DW-28, ruled HIGH after the owner hit it on a real Thai contract
  # clause. It OPENS the epic and is a PRECONDITION of 8.4 -- embedding arbitrary faces widens
  # the fail-closed branch it removes. Numbered 8.0 so 8.1..8.6 keep the keys they already have.
  8-0-a-stacked-thai-mark-reaches-the-page: done
  # 7.10 is sequenced HERE by the owner's call: after 8.0, before 8.1. It does not gate
  # `epic-7: done`, but it must precede the folio-go/v0.1.0 tag (D-7.7.13), and a position in a
  # sequence is the only owner that has worked on this project. Its key stays under epic-7.
  # Key canonicalized to the generator's form (apostrophe -> `-s-`); the story file
  # on disk is 8-1-the-documents-font-chains-become-editable.md. Rename of the key
  # only, per the generator note above -- not a missing story.
  8-1-the-document-s-font-chains-become-editable: done
  8-2-the-chain-editor-sits-where-fonts-are-chosen: done
  8-3-a-font-travels-inside-the-template: done
  8-4-the-engine-renders-from-an-embedded-face: done
  8-4a-the-canvas-paints-with-the-face-the-engine-measured: done
  8-4b-the-canvas-can-name-the-face-the-engine-measured: done
  8-4c-the-designer-ships-the-typeface-it-specifies: done
  8-4e-a-shipped-face-carries-its-identity-to-the-fragment: done
  8-4g-the-bundle-is-a-function-of-the-source-not-of-the-tree: done
  8-4f-a-bound-nobody-can-cross-silently: done
  # 8.4h added 2026-09-02 by Story 8.5's plan gate (D-8.5.13), on D-8.5.1(a)'s exact precedent
  # -- the same move that inserted 8.4f ahead of this same story. It splits the LICENCE GATE out
  # of 8.5 and lands it first. Criterion is independent failure mode, not story budget: measured at
  # lint/internal/manifest/manifest.go:298-301, a font whose licence text does not classify gets
  # "SEE NOTICE" and ResolveAssets returns nil, so A GPL FONT SHIPS WITH A CLEAN BUILD AT HEAD
  # TODAY, with zero catalogue faces in the tree. That is a repair, and a repair does not depend on
  # the extension that surfaced it. Its red-proof must be POPULATION-INDEPENDENT (a synthetic font
  # in a new empty dir, one unclassifiable + one classifiable-but-not-allowlisted, each redding on
  # its own message) or the split buys nothing. NOTE the trap D-8.5.13 measured: manifest.go's two
  # fall-through sites are TWO POPULATIONS UNDER TWO POLICIES -- :298 is fonts (owner's four-id
  # allowlist), :384 is the Thai wordlist (PyThaiNLP, CC0-1.0, NOT one of the four). Both close,
  # against DIFFERENT lists; CC0-1.0 must NOT be added to the owner's four.
  8-4h-the-licence-gate-closes: done
  # 8.4i added 2026-09-02 by Story 8.4h's CLOSE (D-8.4i.1..D-8.4i.6). 8.4h made the gate fail
  # closed; 8.4i makes the classifier stop lying to it. Three measured defects the four review
  # layers missed: (1) DW-125, a LIVE BYPASS -- ClassifyLicenceText returns on the FIRST SPDX line
  # anywhere in a file, so GPL text carrying a stray "SPDX-License-Identifier: MIT" passes the
  # now-fail-closed gate; (2) DW-124, the MIT branch is a greedy catch-all on a grant clause shared
  # by MIT/OFL/UFL, so a near-miss on any name marker lands in MIT rather than FamilyUnknown --
  # and THREE code comments assert a safety that does not exist, one of which the review quoted to
  # REJECT the finding; (3) DW-120, appending "GPL-3.0" to the owner's four-id allowlist reddens
  # nothing, making D-8.5.13's prohibition unenforceable. First task runs the conflict detector
  # REPORT-ONLY over the whole population and records it; only then does it turn fatal.
  # BOUND (D-8.4i.6): 8.4i is the LAST licence-gate insertion this epic gets. Anything further
  # found here routes to Epic 15's release gate (Story 15.3 already owns AD-26's manifest), NOT to
  # an 8.4j -- unless it is a demonstrated live bypass of a gate declared fail-closed.
  8-4i-the-classifier-stops-lying-to-the-gate: done
  # 8.4j added 2026-09-02 at Story 8.4i's CLOSE (D-8.4j.1). DW-131: spdxLineRE captures a SINGLE
  # TOKEN and ClassifyLicenceText never routes to ClassifySPDXExpression, so collect-all works
  # ACROSS lines and not WITHIN one. A font LICENSE carrying "OFL-1.1 OR GPL-3.0-only" classifies
  # (permissive, "OFL-1.1"), sits on the owner's four-id allowlist and PASSES the fail-closed gate;
  # reversed it refuses. Order-dependent -- DW-125's own shape where collect-all does not reach.
  # The sibling ClassifySPDXExpression already resolves that input to copyleft, so D-8.4i.1's
  # stated basis ("make the two functions agree") is only HALF-APPLIED until this lands.
  # It meets D-8.4i.6's exception on all THREE restated conjuncts: a gate this epic declared
  # fail-closed, demonstrated BY PROBE, and reachable by the population the next story adds.
  # ONE defect, ONE fix, ONE red-proof -- nothing else rides it. Red-proof must include a
  # permissive-OR-permissive case that RESOLVES, proving the fix is a classifier and not a ban.
  8-4j-a-compound-licence-line-is-read-whole: done
  # 8.4k added 2026-09-02 (D-8.4k.1). Implements the OWNER's ruling D-8.5.14: the classifier learns
  # <id> WITH <exception>, admitted iff the base is one of the owner's four AND the exception is on a
  # named owner-approved list. NOT a widening of the insertion bound -- that bound governs work the
  # epic discovers about ITSELF (endogenous), and has no authority over work the OWNER adds
  # (exogenous), which is not bounded but PLACED: before the story that consumes it, never in the
  # same commit as that story's population.
  # GATING FIRST TASK (D-8.4k.3): measure the DOMAIN before building the parser. The canonical font
  # WITH declaration is "GPL-2.0-only WITH Font-exception-2.0" -- a COPYLEFT base, refused on the
  # base half -- and permissive font licences do not generally carry exceptions. So the rule may
  # have an EMPTY reachable domain for fonts. If it does, that returns to the OWNER with the
  # measurement in hand, never resolved in the build and never by widening the four ids.
  # 8.4k ABANDONED 2026-09-02 by owner course correction D-000.11: seven hours and 3,567 lines of
  # code, all in lint/, with zero lines advancing Epic 8's stated goal. The WITH capability and both
  # owner rulings (D-8.5.14, D-8.5.15) are registered to EPIC 15's release gate with the lead's full
  # specification intact (D-8.5.16), so it is rebuilt from that rather than re-derived.
  # FLAT PROHIBITION, not a criterion: NO further licence-gate work in Epic 8 for any reason. The
  # previous bound was a criterion, and criteria get satisfied -- its exception fired four times
  # because 8.4h created the very gate the exception was keyed on.
  # PROCUREMENT CONSTRAINT for 8.5 (D-8.5.17): no WITH form is admitted, so procure only faces whose
  # licences carry no WITH expression. Reason is PARSER SCOPE, NOT LICENCE POLICY -- if someone asks
  # for Linux Libertine the answer is "widen the parser", never "that licence is unacceptable".
  8-4k-a-licence-exception-is-read-and-checked: deferred-to-epic-15
  # 8.5's spec exists at ready-for-dev but is SUPERSEDED pending re-plan after 8.4h (D-8.5.13):
  # AC2 and AC7 moved out. AC4 stays HERE deliberately -- the forbidden-host scan asserts that
  # D-8.5.12's decline was honoured, so it belongs with the thing it constrains.
  # CARRIED IN (D-8.4j.2): 8.5's licence census must pin at least ONE COMPOUND-EXPRESSION case, so
  # 8.4j's fix stays fixed when 20 files arrive -- a census keyed only on today's population is a
  # snapshot, not a guard. 8.5 gets NO compound-expression BAN: that is a proxy guard, it would
  # refuse a legitimately dual-licensed permissive face, and it would be a second authority for a
  # property 8.4j's fix already owns.
  8-5-a-curated-catalogue-ships-with-the-designer: done
  8-6-picking-a-family-puts-it-in-the-file: done
  # 8.4d MOVED TO EPIC 15 on 2026-09-02 (D-8.4d.2). The premise that placed it in Epic 8 (D-8.4.24 --
  # "Epic 8 is adding the weight") is MEASURED FALSE: the 21 catalogue faces are 14.16% of the
  # payload, and deleting every one still leaves 13,501,653 bytes -- 1.5x the ~9 MB -- because the
  # engine wasm and the CJK face are 77.4% on their own and both PREDATE Epic 8. The budget overage
  # is a PROGRAM-level fact, not an Epic 8 fact. Not an eighth insertion: it makes Epic 8 SMALLER.
  8-4d-the-size-budget-is-a-number-something-checks: moved-to-epic-15
  epic-8-retrospective: optional

  # Epic 9 added 2026-08-30 (post-MVP). Reported from the designer: BOX properties set on a
  # text element (and on a Rectangle) have no effect. Traced — render.go references neither
  # Background nor Border; the only producer of pagemodel.Rect on the whole render path is
  # tableRects (render.go:1866), fed from the TABLE element's own style (table_render.go:293),
  # so element background/border are inert on text/image/rect/line, and rect/line never reach
  # the page model at all (render_visibility.go's "LATENT, not broken"). No field, property or
  # format version is added: every value this epic starts consuming is one the format already
  # carries and validates, so a document declaring no element background or border must hash
  # identically and the golden corpus stays a witness.
  # CORRECTED 2026-08-30 by Story 15.1 (D-15.1.1, D-R7.5): that claim is TRUE of the box paint
  # and FALSE AS SHIPPED. The same commit also created folio-go/text_alignment.go and wired
  # style.align into the emitter, which moved ALL FOUR statement goldens by +4 bytes per page.
  # The box paint moved nothing; the alignment did. See the second correction below.
  # Both stories implemented 2026-08-30 in the same session that filed the epic.
  # 9.1 also fixed a latent Epic 4 defect it surfaced: internal/pdf/rectdoc.go's
  # appendEdge emitted a stroked edge PREFIX ("m x1 y1 l x2 y2") in a postfix
  # format, so every four-edge border drew as diagonals between opposite corners.
  # No golden moved ON THAT FIX — no document in the corpus renders a stroked edge, and
  # Story 15.1's operator census confirmed the path is never reached rather than assuming it —
  # and the order is now pinned by TestAppendRectContentStreamEdgeOperandsArePostfix.
  # 9.1 ALSO shipped folio-go/text_alignment.go with no story file, no AC and no FR, and that
  # is what moved the corpus (D-15.1.1). Its two ROUNDED branches -- align:"center" and
  # valign:"middle", the file's only geom.ScaleRound site -- were declared by NO fixture and so
  # had zero golden coverage. That was DW-24, and it is CLOSED: Story 7.3 (2026-08-30) re-derived
  # the enumeration, gave each site a falsifier, and registered fixtures/alignment-rounding/.
  # Corrected 2026-09-02 during the Epic 9 reconstruction; the line above had outlived its defect.
  # Story files reconstructed 2026-09-02 from the shipped diff, and reviewed for the first time:
  # nine findings, four repaired (D-9.R), four registered as DW-143..145.
  epic-9: done
  9-1-the-engine-paints-a-component-s-background-and-border: done
  9-2-line-and-rectangle-draw-in-the-designer-and-in-the-pdf: done
  epic-9-retrospective: optional

  # Epic 10 added 2026-08-30 (post-MVP). Reported from the designer: there is no way to set
  # a text element's colour. Traced — `style` carries no colour for text at all: `background`
  # is the box, and pagemodel.TextRun and internal/pdf/textdoc.go never had an ink, so every
  # document this engine ever produced is black. Adds ONE optional format field, style.color,
  # carried end to end (model, parse, serialize, the schema-derived placeholder fence, the
  # page model, the PDF text emitter, the table cascade, the property command, the projection,
  # the TYPOGRAPHY control and the canvas paint). Absent-by-default and emitting nothing when
  # absent, so the existing corpus hashes identically -- though that clause is the NEGATIVE invariant,
  # and it holds. NO fixture declares a colour at all, so the corpus never renders one: the positive
  # coverage is unowned, which is DW-147 and a gating input to Story 15.3.
  # Story file reconstructed 2026-09-02 from the shipped diff, and reviewed for the first time:
  # nine findings, four repaired (D-10.R), four registered as DW-146..148.
  epic-10: done
  10-1-text-prints-in-a-declared-colour: done
  epic-10-retrospective: optional

  # ---------------------------------------------------------------------------
  # SPRINT SEQUENCE for Epics 11-15, agreed 2026-08-30.
  #
  # This file has no sprint field -- sequence is recorded here, and `ready-for-dev`
  # is NOT used to encode it, because that status means "story file created" and no
  # story file exists for epics 7-15 yet. Everything below stays `backlog` until its
  # story file is written.
  #
  #   S1  15.1                 UNBLOCK. main is RED on the cross-target matrix; every
  #                            other epic lands on a baseline nobody has explained.
  #                            Doing this after building on top is strictly harder.
  #   S2  13.1, 13.2, 13.4     One module (folio-designer/src/preview/), highest value
  #                            per unit of work. 13.1 is the smallest useful story in
  #                            the whole backlog and the one an author needs today.
  #                            13.4 joins them (moved from S5, 2026-08-30): it is what
  #                            makes Preview reachable at all without a JSON file, so it
  #                            belongs with the stories that make Preview usable, not
  #                            with the evidence rail. It carries a SCOPE FENCE -- the
  #                            empty-value behaviour is preview-with-no-data only and
  #                            must never become Render's semantics; BINDING_PATH_ABSENT
  #                            stays a located Error (Story 3.6, S8).
  #   S3  15.2                 Make CI's red readable BEFORE the wider surface of
  #                            12/13/14 starts landing in it.
  #   S4  12.1-12.5, 14.1-14.5 Independent of each other; parallelisable. 12.4 and 14.1
  #                            each OPEN with a ruling/rule, not with code. 12.5 follows
  #                            12.1 within the sprint -- the drag commits through 12.1's
  #                            command. 14.5 can go any time; it depends on nothing.
  #                            14.6 (DATA tab fidelity) OPENS with a ruling too: the
  #                            mockup shows no connect control, so pick-binds-immediately
  #                            vs keep-the-explicit-commit is decided before it is built.
  #                            14.7 carries TWO rulings that are not fidelity questions:
  #                            Cancel/Apply implies a local uncommitted buffer (the second
  #                            document model AD-15 forbids), and the design's MILLIMETRES
  #                            contradict the points every other panel shows -- a
  #                            PRODUCT-WIDE unit decision, not a dialog's.
  #                            14.8 FOLLOWS 12.3: 12.3 makes the table's header/rows
  #                            authorable, 14.8 presents them. Do not build 14.8 first --
  #                            it would have to invent storage 12.3 already owns.
  #                            14.9 is the ONE story in Epic 14 with engine work: the
  #                            canvas projection carries only `tableBind` (one string),
  #                            so the canvas CANNOT draw column headers or a specimen
  #                            row today. It adds a PROJECTION field, never a format
  #                            field. 14.10 depends on it -- you cannot click a column
  #                            the canvas does not draw -- and on 14.6 for the DATA
  #                            panel that offers the row-scope fields.
  #                            Owner instruction (2026-08-30): column binding belongs in
  #                            the MAIN WINDOW; the table editor keeps structure.
  #   S5  13.3, 13.5           The preview evidence rail and the chrome around it: the
  #                            real build in Epic 13. 13.3 carries a RULING -- the
  #                            design prints "Matches native render / Identical bytes on
  #                            darwin/arm64, linux/arm64, linux/amd64" and the TAB CANNOT
  #                            KNOW THAT. Cross-target identity is a property of the
  #                            release proven by CI's matrix, not a live comparison, so
  #                            the wording is earned or reworded before it is drawn.
  #   S6  11.1-11.3            GATED on the owner's realize-vs-retire ruling (see below).
  #                            Sequenced last of the feature work because it is the only
  #                            epic that can move golden hashes, so it wants the S1
  #                            baseline settled underneath it.
  #   S7  15.3                 LAST BY CONSTRUCTION -- it freezes the public API, so it
  #                            follows whichever epics are ruled inside v0.1.0.
  #
  # Readiness gate: CONCERNS, not PASS. Three findings, none of which block sequencing:
  #   1. Epic 11 carries a forward dependency on a decision NOTHING RECORDS. SPEC-fonts
  #      records the question ("Do bold and italic get realized in this scope?"); no
  #      artifact records an answer. 11.1 must not enter a sprint before it is ruled.
  #   2. Epics 9 and 10 are `review` with no story files and no retrospective, and 15.1
  #      most likely attributes the moved goldens to one of them -- so the evidence 15.1
  #      needs is the evidence those two epics never wrote down.
  #      SETTLED 2026-08-30 by Story 15.1 (D-15.1.1): ALL FOUR statement goldens moved, not
  #      statement-1 alone, and EPIC 9 ALONE is implicated -- Epic 10 (304442f) moved nothing,
  #      because the text-ink bracket emits zero bytes when no colour is declared.
  #   3. Epic 8's prose still reads "Bold and italic are NOT in this epic ... a
  #      face-inventory decision SPEC-fonts leaves open". Epic 11 now owns it; that
  #      sentence should point there rather than dangle.
  #
  # Generator note (2026-08-30): `sprint_plan.py generate` was run against this file and
  # its output was DISCARDED, restoring this file from backup. It did exactly what the
  # header above warns of -- dropped 2-3a/2-5a/2-6a/3-1a -- and additionally stripped 87
  # of 100 inline comments, including that header. What was KEPT from the run is its key
  # derivation: seven story keys here were renamed by hand to the script's canonical form
  # (64-char truncation; an apostrophe becomes `-s-`, e.g. `a-table-s-header`), so a
  # future --dry-run reports those keys as in-sync rather than as renames.
  # ---------------------------------------------------------------------------
  # Epics 11-15 added 2026-08-30 (post-MVP). Scoped in conversation from a designer-UI
  # and release-readiness audit; every finding was reproduced against the running app or
  # the live CI before it was written up. Like Epics 7-10 these are DELIBERATELY authored
  # by hand rather than generated -- see the header warning above.
  #
  # 11's blocking fork was RULED 2026-09-06: realize, not retire (D-A, epic-11-14-decision-log.md).
  # 11.1 shipped seven faces, not the nine D-A assumed -- upstream publishes no Thai italic.
  # SPEC-fonts still records the question as open and is the stale copy.
  epic-11: done
  11-1-the-shipped-families-gain-a-weighted-and-a-sloped-face: done
  11-2-the-engine-resolves-a-face-from-the-declared-weight-and-slop: done
  11-3-the-canvas-paints-the-weight-the-engine-resolved: done
  # 11.4 added 2026-09-06 by OWNER DECISION D-11.0.1, which took the fourth option over the
  # recommended triggered deferral. D-11.2.1 rules that the engine resolves a weight only to a face
  # the document explicitly NAMES and never infers one -- the only route that does not break AD-8. So
  # without 11.4 the ONLY document that could bold would be starter.folio, and only because 11.3
  # edits it. 11.4 is what makes bold reachable in a document the author already has. It touches the
  # family control that Epic 16 rebuilt and Epic 14 will rebuild again; the owner priced that.
  11-4-picking-a-family-declares-the-cuts-it-has: done
  # 11.5 SPLIT OUT OF 11.3 on 2026-09-06 by D-11.2.12. It was the only item in 11.3 that moves a
  # rendered byte, and it was forcing the full four-target matrix onto an otherwise designer-side
  # story; split out, 11.3 runs no matrix at all. This is the INVERSE of D-11.1.18, where splitting
  # would have multiplied the same gate -- the two rulings are consistent, not contradictory.
  # Depends on 11.2 alone, NOT on the canvas, so it may run before or after 11.3/11.4.
  # It HALTED for an owner attestation -- DISCHARGED 2026-09-06; signoff.json records the reading.
  # The "AD-21 / D-4.7.1" citation above is WRONG: the obligation is doctrinal (D-000.22 -> D-2.3.5).
  # Corrected by D-11.5.2 in epic-11-14-decision-log.md; epics.md is fixed, this line is where it travelled.
  11-5-a-bold-document-is-a-pinned-golden: done
  epic-11-retrospective: optional

  # 12.4's ruling is SETTLED: D-12.4.1 (owner, 2026-09-05, epic-11-14-decision-log.md).
  # style.padding stays an engine property — a loaded document keeps and renders it, the panel
  # never authors it, and the Go command layer refuses padding* on non-table kinds. The old
  # two-way fork (insets vs table-only) is dead, and 14.8's cell-padding AC is dropped with it.
  epic-12: done
  12-1-the-page-header-and-page-footer-take-the-height-the-author-s: done
  12-2-the-document-declares-its-locale-and-its-utc-offset: done
  12-3-a-table-s-header-and-its-alternating-rows-are-authorable: done
  12-4-padding-is-honoured-outside-a-table-or-the-format-says-it-is: done
  12-5-a-band-boundary-is-dragged-on-the-canvas: done
  epic-12-retrospective: optional

  epic-13: done
  13-1-the-preview-keeps-the-pdf: done
  13-2-the-viewer-navigates-like-a-pdf-viewer: done
  13-3-the-preview-screen-is-the-evidence-screen: done
  13-4-preview-runs-without-sample-data-and-an-absent-value-is-empt: done
  13-5-the-chrome-tells-the-truth-about-the-preview: done
  13-6-the-preview-navigates-by-page-thumbnails: done
  epic-13-retrospective: optional

  # 14.1 writes the button rule FIRST; the sweep is its consequence, not its substance.
  # Boundary gate run 2026-09-10 at 09a41e4, CONCLUDED -- 4 items concluded, 1 unconcludable.
  # Done WITH THREE UNMET ITEMS CARRIED, not clear: UX-DR25 keyboard reach for table columns
  # (DW-387), DW-383 still uncovered (the border render path has never been byte-compared on
  # any target), and DW-385 (the canvas drawing reaches assistive technology not at all).
  # The gate also found three register entries measurably wrong; DW-356 needs RE-BASING, not
  # closing -- its subject was deleted at bd634bd. See epic-11-14-decision-log.md D-14.11.1.
  epic-14: done
  14-1-one-button-vocabulary: done
  14-2-a-line-is-a-thickness-and-a-colour-a-rectangle-is-a-fill-and: done
  14-3-a-placed-component-is-the-selected-component: done
  14-4-the-panel-offers-no-control-the-engine-will-refuse: done
  14-5-the-product-wears-its-own-mark: done
  14-6-the-data-tab-is-the-binding-panel-the-design-drew: done
  14-7-the-table-editor-is-the-matrix-the-design-drew: done
  14-7b-the-table-editor-s-cancel-discards-what-it-counted: done
  14-8-the-table-editor-carries-the-header-cell-and-border-sections: done
  14-9-the-canvas-draws-the-table-it-will-print: done
  14-10-a-table-column-is-bound-from-the-main-window: done
  epic-14-retrospective: optional

  # Epic 15 is the release blocker set, and 15.1 was URGENT rather than merely first.
  # AS FOUND (before 15.1 ran): main was RED on the cross-target matrix. ALL FOUR golden
  # statements had moved -- statement-1, -5, -20 and -50 -- each by exactly +4 bytes per page
  # (+4/+20/+80/+200); statement-1, the primary acceptance fixture, produced 114df1d6...
  # against a then-recorded ef58bbf6...  All four targets agreed throughout, so CAP-13/S2/S3
  # never lapsed; what had failed was that a golden moved unexplained, which C6 makes a defect
  # until proven intended.
  # AS IT STANDS NOW: 114df1d6... IS statement-1's recorded digest, and all four targets agree
  # on it and on the other three. The matrix is still red, but for an intended reason -- the
  # human sign-off is pending re-attestation. There is no longer an unexplained hash.
  # EPIC 9 ALONE is implicated (D-15.1.1): 791ed00 created folio-go/text_alignment.go and
  # wired style.align to the emitter, moving one Tm x-operand on the per-page footer element.
  # Epic 10 (304442f) moved nothing. The move is INTENDED (D-R7.6) and the goldens are
  # re-recorded; the human sign-off is re-attested, never patched.
  # 15.1 pulled forward as story zero of the Epic 7-8 run (D-R7.0): both epics guard
  # themselves with "the corpus hashes identically", unevaluable while it is red.
  # Ruling and attribution in epic-7-8-decision-log.md (D-R7.5, D-R7.6).
  epic-15: in-progress
  # 15.1 CLOSED 2026-08-30 at 36bb3f5: attribution D-15.1.1, four goldens re-recorded on the
  # owner's ruling (D-R7.6), and the human sign-off written by the owner themselves. Full record,
  # measured gates and deferrals in the story's ## Delivery Log; DW-24 was open here, owner Story 7.3; it CLOSED 2026-08-30 in that story (see
  # deferred-work.md). This line is kept as written and corrected rather than edited.
  15-1-the-golden-report-s-moved-hash-is-explained: done
  # 15.0 added 2026-09-02 by OWNER DECISION D-8.4d.1, which REVERSES D-8.5.12: catalogue faces are
  # FETCHED FROM THE RELEASE'S OWN ORIGIN ON FIRST PICK, not precached at first load. Placed here
  # (D-8.4d.2) because it changes OFFLINE BEHAVIOUR -- NFR7/AD-19, a release-facing property -- and
  # because D-000.14's format/shape freedom expires at 15.3. Exogenous work: unbounded but PLACED.
  # Cost, always quoted WITH its mitigant: a family not yet fetched cannot be picked while offline,
  # but the catalogue is a PALETTE not coverage (the three Noto faces are the coverage), so it
  # degrades to "you cannot pick that family right now", never to a document that will not render --
  # and a face already embedded travels inside the .folio (Story 8.6), so nothing regresses.
  # SPEC.md Non-goals and font-catalogue.md were corrected AHEAD of it at 3a40c38 (D-000.10's
  # precedent), policy only, mechanism left to the story.
  15-0-a-catalogue-face-arrives-when-it-is-picked: backlog
  # 8.4d lands HERE, AFTER 15.0 -- the fetch tier means the weight is not finished, and setting a
  # threshold you intend to move next story is the tuning-to-fit D-8.4.24 forbade. An interim
  # ratchet was CONSIDERED AND REJECTED: it must be bumped by every byte-moving story, so across
  # Epics 9-14 it is ~20 chances to bump it thoughtlessly. A gate that gets routinely bumped is not
  # a gate; it is a chore that teaches people to bump gates. The ungated window is PRICED: 8.6 moved
  # the payload 0.06%, Epics 9/10 add zero bytes, 77.4% is fixed, and 8.5's per-asset Brotli record
  # runs every build -- so 8.4d inherits a series, not an archaeology problem.
  15-2-ci-s-red-means-something: backlog
  # 15.2a added 2026-08-31: DW-32, raised to HIGH at Story 8.2's plan gate when the mechanism
  # was re-measured as command-shape INJECTION rather than a bad value. Its Go half narrows the
  # exported ApplyComponentCommand and has not shipped, so it JOINS D-7.8.3's before-the-tag set --
  # which is now TWO items, not one. Sequenced before 15.3 so the deadline is satisfied by
  # construction rather than by a promise.
  # 15.2b added 2026-09-05 by D-15.2.1. Dispatched EARLY, alongside 12.3 -- it lives in Epic 15 by
  # subject but every story dispatched before it carries the tracked-only scan hole (D-000.21).
  15-2b-the-font-host-scan-sees-the-whole-tree: done
  15-2a-a-component-command-means-exactly-what-it-names: done
  # 8.4d and 8.4k are EPIC 15's, and this list is where a reader looks for Epic 15's work plan.
  # Their rows live under Epic 8 carrying `moved-to-epic-15` / `deferred-to-epic-15` and their text
  # is in Epic 8's section of epics.md, so Epic 15's own rows under-counted it by two. Recorded here
  # 2026-09-06 (lead's grounding refresh) as POINTERS -- the authoritative rows stay at lines ~284
  # and ~300 so no story has two status values. 8.4d is sequenced BEFORE 15.3 cuts the tag.
  # ^ 2026-09-16: 15.3 is DEPRECATED (below), so that ordering now binds the tag story in
  #   spec-client-libraries/stories.yaml instead. 8.4d AND 8.4k both still precede the tag.
  #   -> 8-4d-the-size-budget-is-a-number-something-checks   (row above, `moved-to-epic-15`)
  #   -> 8-4k-a-licence-exception-is-read-and-checked        (row above, `deferred-to-epic-15`)
  # ^ 2026-09-17 (OWNER RULING, recorded by spec-client-libraries story 2): 8.4d and 8.4k are
  #   RELEASED from the folio8-go/v1.0.0 tag gate -- designer-release and `lint` work, not Go-tag
  #   prerequisites. The ordering lines above no longer bind the tag. 8.4d still owes its
  #   size-budget ruling. Rows and statuses unchanged.
  # 15.3 DEPRECATED 2026-09-16 by OWNER DECISION. Cutting the engine tag moves to
  # _bmad-output/specs/spec-client-libraries/stories.yaml, where it is re-scoped: the tag is
  # folio8-go/v1.0.0, not v0.1.0, and it is cut only AFTER the designer surface (canvas types,
  # the wasm package) leaves the public API, so v1.0.0 freezes render + validate alone.
  # INHERITED by that story, so none of 15.3's obligations evaporate: DW-4's re-measure of the
  # surface census and re-affirmed trigger, the engineering-lead checkpoint DW-4 records as
  # discharged by the tagging story, and the ordering that 8.4d and 8.4k precede the tag.
  15-3-folio-go-v0-1-0-is-cut: deprecated
  epic-15-retrospective: optional

  # Epic 16 added 2026-09-02 by OWNER DECISION D-16.1; its rulings, measurements and
  # reversals are in epic-16-decision-log.md, and each story states its own in its spec.
  epic-16: done
  16-0-the-embed-boundary-stops-throwing: done
  16-1b-the-binary-is-asked-what-it-says-about-itself: done
  16-1-a-font-arrives-from-the-web-with-its-terms-attached: done
  16-1a-the-local-face-tier-covers-the-head-of-the-library: done
  16-2-a-fetched-face-stays-on-this-machine: done
  16-3-the-font-browser-is-the-dialog-the-design-drew: done
  # 16.4 also fixes 8.6's deferred listbox finding and re-derives its disk-font decline against D-16.1;
  # both are stated in 16-4-the-family-control-names-three-sources.md and in deferred-work.md.
  # 16.5 added 2026-09-03 by OWNER DECISION D-16.R.46: install and embed are separated. It runs BETWEEN
  # 16.3 and 16.4 despite its number -- 16.4's three groups mean what 16.5 defines, so building 16.4
  # first would build the control twice. Embed-on-use, designer-side only; prune-on-save was refused.
  16-5-installing-a-face-is-not-embedding-it: done
  16-4-the-family-control-names-three-sources: done
  # 16.6 added 2026-09-03 by OWNER DECISION (D-16.R.82), looking at the shipped panel: the machine
  # font store section is not necessary. It is DELETED, and per-face removal goes with it -- the
  # owner's choice among three arms, made knowing the capability is lost. Supporting measurement:
  # 0 of the 6 mockup files draw this panel at all, so deleting it moves the screen TOWARD the
  # design. Consequences the story must carry: both refusal sentences stop naming a removal
  # control (App.tsx lateEmbedRefusal, font-store.ts storeWriteRefusal), and DW-175 -- bytes
  # outliving their face record with nothing able to free them -- becomes PERMANENT rather than
  # pending, because its owner was 'the first story that adds store maintenance' and there will
  # now be none. Sequenced AFTER 16.4 closes: 16.4 is in review with a frozen contract that does
  # not cover deleting a panel, and folding this in would invalidate the review just completed.
  16-6-the-machine-store-panel-goes-away: done
  # 16.7 added 2026-09-03 by OWNER, from the design: each row shows the typeface it names -- name
  # left, specimen right, script-aware. The font browser already does this correctly and the menu
  # inherits its mechanism AND its rule that a sample is never set in a substitute face. Three
  # defaults are the owner's to overturn at the plan gate: the web group gets NO specimen (fetching
  # 1,273 families to draw a menu is what this epic keeps refusing), the specimen replaces the
  # per-row note in the first two groups (narrowing D-16.R.72's three carriers to two there), and a
  # declared chain shows the face it paints with. Needs a browser run: no test here can see whether
  # a specimen rendered in its own face.
  16-7-every-row-shows-the-typeface-it-names: done
  # 16.8 added 2026-09-04 by OWNER: `body` is internal vocabulary standing where a typeface name
  # belongs. Offered three shapes -- rename the chain to Noto Sans (free), embed Roboto in the
  # starter (+348 KB per document), or SHIP Roboto in the engine -- the owner chose the third,
  # knowing the payload grows ~348 KB for every user whether they use it or not. Roboto is OFL-1.1
  # (relicensed 2024), already an admitted identifier, so the licence boundary is not a blocker.
  # THE RISK IS ONE ROBOTO: the catalogue cut (347.6 KB, e688a215…) and the testdata cut (167.7 KB,
  # 79e85140…) are different bytes under one name, and shipping the wrong one would render rather
  # than fail. A digest assertion is a task, not a note.
  16-8-a-new-document-opens-in-a-typeface-with-a-name: done
  # 16.9 added 2026-09-04 by OWNER, four items from the running panel: remove the font-chain
  # EDITOR (the data stays -- it is what makes Thai and CJK fall through, and 16.8 just shipped it),
  # remove Clear font family (text always has a typeface), remove a dropdown group, and centre the
  # field's chevron. NOTE THE REVERSAL: the written request named AVAILABLE LOCALLY; the owner's
  # answer named AVAILABLE TO INSTALL, and the answer is taken as the decision -- the browser is
  # where installing happens, so the menu keeps what is usable now. Flagged for correction at the
  # gate. This DISCHARGES the 30s/180s dropdown-block deferral: the pick that could block is gone.
  16-9-the-typography-panel-offers-what-you-can-use: done
  # 16.10 added 2026-09-04 by OWNER: remove the disclosure paragraph from the FONT BROWSER header
  # (FontBrowser.tsx:287), "follow the design strictly". MEASURED: the mockup contains NONE of that
  # prose -- 0 occurrences of "families you can add" / "snapshot taken" / "variable file are not
  # shown", against a positive control that hits. It is an invention, not a transcription. The same
  # sentence was already cut from the dropdown, so this is its LAST production consumer:
  # familyIndexDisclosure() becomes dead, and 16.9's own rule (deleted code is deleted) says the
  # function and its tests go with it, plus the orphaned .font-browser-disclosure rule.
  # DISCHARGES the registered familyIndexDisclosure count divergence (31 vs 31-plus-stored) -- the
  # sentence that disagreed will not exist. Does NOT discharge the resultLine defect ("1274 of
  # 1273"): resultLine reads addableFamilyCount and STAYS. Spec deliberately not written yet --
  # 16.9 is moving the test files this story would anchor into, and stale anchors are D-16.R.28.
  16-10-the-browser-header-says-what-the-design-says: done
  # 16.11 added 2026-09-05 as a REPAIR, under D-000.12(3): a repair may join an epic that has not
  # closed, an extension may not. Story 16.8 grew fonts.Shipped() to four and made a catalogue
  # family the starter's only declared chain; four things closed over the old state. One failed
  # loudly and is fixed (28cd225); this story re-derives the other three from their sources, plus
  # a fourth assertion 16.10 made permanently unsatisfiable. Epic 16 closes once this guard exists
  # (D-000.12(4)) -- closing it over a guard vacuous about its own headline face would make the
  # gate ceremonial. No product behaviour changes.
  16-11-what-closes-over-a-moving-set-is-re-derived: done
  epic-16-retrospective: optional
  # ==========================================================================
  # EPIC 17 -- The designer's editing controls behave the way an editor behaves
  # Four items raised by the OWNER on 2026-09-04 from using the running designer.
  # None is font work; this is Epic 14's lane ("the designer's controls read as
  # one product"), reopened as a new epic rather than reused -- Epic 14 is
  # closed, and a closed epic's spec is not a place to add scope.
  # ==========================================================================
  # epic-17 closed 2026-09-06. All six stories done; NO story defers anything to a boundary gate
  # (measured: 0 mentions across all six specs, against a control of 6 in 12.5's). It never had a
  # gate artifact and none is owed. Epic 12's gate ran the matrix and Playwright at a HEAD that
  # contains all Epic 17 work, so its code is covered incidentally rather than by its own gate --
  # recorded because that is weaker than every other closed epic's evidence. See D-000.29.
  epic-17: done
  # 17.1 OWNER DECISION TAKEN AT THE GATE, and it is the cheap option knowingly:
  # DEBOUNCE ~200ms AND SEND A REAL COMMAND. The canvas paints only the engine's
  # projection -- AD-17 forbids the browser laying out text itself -- so "live"
  # necessarily means a round trip. Three costlier options were put to the owner
  # with their costs (a non-committing preview op; per-keystroke commands
  # coalesced in undo; per-keystroke commands with one undo step each). Debounce
  # was chosen. CONSEQUENCE TO HOLD AT THE GATE: the canvas updates shortly
  # AFTER typing stops, not while typing, so the acceptance wording must not
  # promise per-keystroke painting.
  17-1-the-canvas-follows-the-content-field: done
  # 17.2 Clicking the empty space around the page must KEEP the selection. Watch
  # what else relies on the clear -- Escape-to-deselect and keyboard navigation
  # are the likely dependents, and a test asserting the clear is a guard to
  # CORRECT, not to delete.
  17-2-clicking-beside-the-page-keeps-the-selection: done
  # 17.3 OWNER DECISION: font size and leading show the DEFAULT AS A REAL VALUE
  # and it is WRITTEN INTO THE DOCUMENT, not merely displayed -- chosen over
  # display-only knowing it changes saved bytes.
  # SAFETY PROPERTY THE SPEC MUST CARRY, assumed here and flagged for the gate:
  # OPENING a document may never mutate it. The value is written when the author
  # COMMITS the field, never on load, or every existing .folio rewrites itself
  # on being looked at. The 24 golden digests carry their own input.folio and
  # are not re-saved by the designer, so they must be UNMOVED -- that is the
  # proof this stayed inside its lane.
  17-3-size-and-leading-show-the-value-they-use: done
  # 17.4 Up/down arrows step a number field. Note the internal representation:
  # points are stored in THOUSANDTHS, so "the smallest meaningful step" is a
  # decision per unit, not a constant -- 1pt and 0.1 leading are the likely
  # answers, but the step must be derived from the field, not hardcoded once.
  # REVERSAL RECORDED 2026-09-04 (ruled by the ORCHESTRATOR on Story 17.3, not
  # by the owner; flagged to the owner in 17.3's report). 17.4 shipped ONE
  # predicate closing two rows -- an UNSET field and a MIXED selection both
  # present as an empty draft, so neither steps and neither sends a command.
  # 17.3 removed the precondition the UNSET half rested on: fontSize and
  # lineSpacing now carry the engine's own effective value as text, so the
  # draft parses and the arrow steps it. The unset arm is RETIRED, in 17.3's
  # commit, with 17.4's test rewritten to the new behaviour. THE MIXED ARM
  # SURVIVES UNCHANGED -- stepping a mixed selection still flattens every
  # component onto one value, and 17.3 touches nothing about that. The reason
  # the arm went is 17.4's own property: the arrow and the keyboard are the
  # same act (expect(stepped).toEqual(typed)), and keeping the guard would have
  # made typing 1.1 into a box reading 1 write 1.1 while ArrowUp on the same
  # visible 1 wrote nothing. This is a guard retiring with its world, not a
  # decision overturned; NOT recorded in epic-16-decision-log.md, which is
  # Epic 16's.
  17-4-arrow-keys-step-a-number-field: done
  # 17.5 Raised by the OWNER on 2026-09-04 from the running designer: the CONTENT
  # box resizes, but only by the BROWSER's own diagonal grip in its bottom-right
  # corner -- the one control in the inspector this application does not draw.
  # Replace it with the field's own bottom edge as the handle. TWO THINGS TO HOLD
  # AT THE GATE. (a) The height is VIEW state: a drag must send no command and
  # change no saved bytes -- and this is the row Story 17.1 just put a ~200ms
  # commit timer under, where `blur` is a commit path, so a handle that steals
  # focus turns a resize into a write. (b) `property-prose-height.test.ts`
  # asserts `resize: vertical` today; it is a guard to CORRECT, not to delete --
  # it exists because a height contract on the shared `.property-value-prose`
  # class once rendered the font-family combobox at twice its size.
  # ONE DECISION LEFT TO THE BUILD, to be stated and tested rather than assumed:
  # whether an author's height survives a change of selection.
  17-5-the-content-box-resizes-from-its-bottom-edge: done
  # 17.6 IS WAVE A OF THE EPICS 11-14 RUN, registered under Epic 17 because it is
  # designer test infrastructure and this is the live designer epic. The epic
  # title does not describe it; this comment is the pointer, not a rename.
  # Ruled by the engineering lead as D-C (epic-11-14-decision-log.md): the AD-17
  # corpus scan has been standing red since DW-152, so a SECOND violation changes
  # the failure's CONTENTS and not its STATUS, and no gate reads contents.
  # Fifteen stories of designer UI are about to land in exactly the population
  # that scan polices. PREFERRED REPAIR is a named-owner exception, not a rewrite
  # of the assertion -- see D-C's forbidden branch.
  17-6-the-ad-17-scan-can-see-a-new-violation: done
