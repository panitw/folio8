# The root .gitignore excludes *.log, which is right for build noise and wrong
# for these: they ARE the record. dotnet-linux-soak.md's tables are read out of
# these files, and a verdict whose console output was discarded is a verdict
# retyped from a terminal that no longer exists. Scoped to this directory only.
!*.log
