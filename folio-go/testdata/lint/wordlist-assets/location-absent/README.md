# location-absent fixture

Deliberately empty of a `folio-go/internal/text/wordlist/` subtree: this
fixture root stands in for a repo state that predates Story 2.1
(AC9's guard must not treat a missing declared location as itself a
violation — see `TestWordlistAssetsFixtureScan/location-absent`).

This README exists only so git tracks this otherwise-empty directory.
